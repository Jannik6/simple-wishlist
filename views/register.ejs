<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Neuen Benutzer erstellen</title>
    <link rel="icon" type="image/x-icon" href="/images/<%= listType %>">
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <div class="container" style="max-width: 500px; margin: 0 auto; padding-top: 2rem;">
        <% if (messages.error && messages.error.length > 0) { %>
            <div class="alert alert-danger">
                <%= messages.error %>
            </div>
        <% } %>

        <% if (messages.success && messages.success.length > 0) { %>
            <div class="alert alert-success">
                <%= messages.success %>
            </div>
        <% } %>

        <h1 style="text-align: center;">Erstelle eine neue Wunschliste</h1>
        <p style="text-align: center; color: var(--text-muted); margin-bottom: 2rem;">
            Erstelle einen Benutzer mit einer neuen Wunschliste
        </p>

        <form action="/register" method="POST" id="register-form">
            <label for="username">Username:</label>
            <input type="text" id="username" name="username" placeholder="Choose a username" autocomplete="username" required autofocus data-lpignore="true" minlength="3">
            <small style="color: var(--text-muted); margin-top: -1.5rem; margin-bottom: 1rem; display: block;">Mindestens 3 Zeichen</small>

            <label for="password">Password:</label>
            <input type="password" id="password" name="password" placeholder="Enter a password" autocomplete="new-password" required data-lpignore="true" minlength="6">
            <small style="color: var(--text-muted); margin-top: -1.5rem; margin-bottom: 1rem; display: block;">Mindestens 6 Zeichen</small>
            
            <button type="submit" style="width: 100%;">Account erstellen</button>
        </form>

        <p style="text-align: center; margin-top: 1.5rem; color: var(--text-muted);">
            Du hast bereits einen Account? <br>
            <a href="/" style="color: var(--primary-color); text-decoration: none; font-weight: 500;">Zurück zur Wunschlistenauswahl</a>
        </p>
    </div>
</body>
</html>
