<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" type="image/x-icon" href="/images/<%= listType %>">
    <title>Select Wishlist</title>
    <link rel="stylesheet" href="/styles.css">
    <style>
        .user-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 1.5rem;
            margin-top: 2rem;
        }

        .user-card {
            background-color: var(--white);
            border: 2px solid var(--border-color);
            border-radius: 12px;
            padding: 2rem;
            text-align: center;
            transition: all 0.3s ease;
            cursor: pointer;
            box-shadow: var(--shadow);
        }

        .user-card:hover {
            border-color: var(--primary-color);
            box-shadow: var(--shadow-lg);
            transform: translateY(-4px);
        }

        .user-card h2 {
            margin: 0 0 1rem 0;
            color: var(--text-color);
            font-size: 1.5rem;
        }

        .user-card p {
            color: var(--text-muted);
            margin-bottom: 1.5rem;
        }

        .user-card a {
            display: inline-block;
            background-color: var(--primary-color);
            color: var(--white);
            padding: 0.75rem 1.5rem;
            border-radius: 6px;
            text-decoration: none;
            font-weight: 500;
            transition: background-color 0.2s ease;
        }

        .user-card a:hover {
            background-color: var(--primary-hover);
        }

        .user-select-container {
            text-align: center;
            margin-bottom: 2rem;
        }

        .user-select-container h1 {
            margin-bottom: 0.5rem;
        }

        .user-select-container p {
            color: var(--text-muted);
            font-size: 1.1rem;
        }

        @media screen and (max-width: 768px) {
            .user-grid {
                grid-template-columns: 1fr;
                gap: 1rem;
            }

            .user-card {
                padding: 1.5rem;
            }

            .user-card h2 {
                font-size: 1.3rem;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="user-select-container">
            <h1>Select a Wishlist</h1>
            <p>Choose from the available wishlists below</p>
        </div>

        <% if (users.length > 0) { %>
            <div class="user-grid">
                <% users.forEach(user => { %>
                <div class="user-card" onclick="window.location.href='/<%= user.username %>/';">
                    <h2><%= user.username.charAt(0).toUpperCase() + user.username.slice(1) %></h2>
                </div>
                <% }); %>
            </div>
        <% } else { %>
            <div style="text-align: center; padding: 2rem;">
                <p style="color: var(--text-muted); font-size: 1.1rem;">No wishlists available yet.</p>
            </div>
        <% } %>

        <div style="text-align: center; margin-top: 2rem; padding-top: 2rem; border-top: 1px solid var(--border-color);">
            <a href="/register" style="background-color: var(--success-color); color: var(--white); padding: 0.75rem 1.5rem; border-radius: 6px; text-decoration: none; font-weight: 500; display: inline-block; transition: background-color 0.2s ease; margin-right: 0.75rem;">
                + Create New Wishlist
            </a>
            <a href="/login" style="background-color: var(--primary-color); color: var(--white); padding: 0.75rem 1.5rem; border-radius: 6px; text-decoration: none; font-weight: 500; display: inline-block; transition: background-color 0.2s ease;">
                Login
            </a>
        </div>
    </div>
</body>
</html>
